body {
    font-family: Arial, sans-serif;
    padding: 10px;
}

#product-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
}

.product {
    border: 1px solid #ccc;
    padding: 8px;
    text-align: center;
}

.product img {
    width: 80px;
    height: 80px;
    object-fit: cover;
}

button {
    background: green;
    color: white;
    padding: 8px;
    border: none;
    cursor: pointer;
}
